## 困境（dilemma / deadlock / impasse / stuck）

- <口>：困境、两难，强调在两个不理想的选择之间难以抉择
  - We're facing a **dilemma**: ship now with bugs, or delay and miss the deadline.
  - 我们面临一个**困境**：现在发布有 bug，或者延期错过截止日期。

- <通>：死锁、僵局，特指两个或多个进程互相等待资源而卡住
  - The system entered a **deadlock** when two transactions locked the same tables.
  - 当两个事务锁定了相同的表时，系统进入了**死锁**。

- <书>：僵局、绝境，强调无法突破、停滞不前的状态
  - We've reached an **impasse** in the negotiation with the client.
  - 我们和客户的谈判陷入了**僵局**。

- <口>：卡住、陷入困境，强调无法继续推进的状态
  - I'm **stuck** on this bug—can you take a look?
  - 我被这个 bug **卡住**了——你能帮我看一下吗？
## 目录（directory / catalog / table of contents / index）

- <通>：目录、**文件夹**，最常用，指文件系统中的文件夹
  - The configuration files are stored in the **/etc** **directory**.
  - 配置文件存储在 **/etc** **目录**下。

- <通>：目录、**清单**，指系统性地组织的条目列表，如产品、文档或资源
  - We maintain a **catalog** of all available microservices.
  - 我们维护了所有可用微服务的**目录**。

- <书>：目录，特指文档、书籍或手册开头的**章节列表**
  - Please refer to the **table of contents** for chapter details.
  - 请参阅**目录**了解章节详情。

- <书>：**索引**，特指文档末尾的关键词索引，用于快速查找
  - Check the **index** at the back of the manual to find the logging section.
  - 查阅手册末尾的**索引**，找到日志相关章节。
## 能力（ability / capability / capacity / competence）

- <通>：能力，最通用，强调具备做某事的技能或特点
  - The system has the **ability** to scale horizontally.
  - 该系统具备水平扩展的**能力**。

- <书>：能力、性能，正式书面，强调系统或工具所具备的功能性
  - This framework provides advanced logging **capabilities**.
  - 该框架提供了高级日志记录**能力**。

- <书>：容量、能力，强调系统或组件能够处理的最大量
  - The server's **capacity** is limited to 10,000 concurrent connections.
  - 该服务器的**能力**仅限于一万并发连接。

- <书>：能力、胜任力，正式书面，强调具备某领域的专业能力
  - She has demonstrated strong technical **competence**.
  - 她展现了出色的技术**能力**。
## 指令（instruction / directive / command / order）

- <通>：指令、说明，强调**具体的**操作指引或步骤说明
  - Follow the deployment **instructions** carefully.
  - 仔细遵循部署**指令**。

- <书>：指示、指令，强调从**上级**或管理层下达的**正式**要求
  - The team received a **directive** to migrate all services to the new platform.
  - 团队收到了迁移所有服务到新平台的**指令**。

- <通>：命令、指令，强调计算机系统中执行的具体操作命令
  - Use the `docker build` **command** to create the image.
  - 使用 `docker build` **命令**来创建镜像。

- <书>：命令、指令，强调必须服从的权威性要求，语气最强
  - The system will execute the **order** to restart all nodes.
  - 系统将执行重启所有节点的**命令**。
## 方面/角度（aspect / angle / perspective / dimension）

- <通>：方面，最通用，指事物的某个侧面或组成部分
  - We need to consider the security **aspect** of this feature.
  - 我们需要考虑该功能的**安全方面**。

- <口>：角度，强调从某个出发点或立场来看问题
  - Let's look at this issue from a performance **angle**.
  - 让我们从性能**角度**来看这个问题。

- <书>：视角、角度，强调观点或立场，偏正式书面
  - From an engineering **perspective**, this design is solid.
  - 从工程**角度**来看，这个设计是扎实的。

- <书>：维度、方面，强调从不同度量或层面来考量
  - We should evaluate this from a cost **dimension** as well.
  - 我们也应该从成本**维度**来评估这一点。
## 方法/方案（way / approach / solution / method）

- <口>：方式、方法，最通用，表做某事的方式或手段
  - One **way** to improve performance is to use caching.
  - 提升性能的一个**方法**是使用缓存。

- <书>：方法、思路，正式，强调处理问题的方式或角度
  - We need a systematic **approach** to debugging this issue.
  - 我们需要一套系统性的**方法**来调试这个问题。

- <通>：解决方案，强调用来解决具体问题的成熟方案
  - This is the best **solution** for handling concurrent requests.
  - 这是处理并发请求的最佳**方案**。

- <书>：方法、流程，强调按步骤、有体系地做某事
  - This testing **method** ensures high code coverage.
  - 这种测试**方法**能确保高代码覆盖率。
## 保障/保证（guarantee / assurance / safeguard）

- <通>：保证、担保，强调明确承诺某事一定会发生或某条件一定满足
  - This protocol **guarantees** exactly-once delivery.
  - 该协议**保证**精确一次投递。

- <书>：保证、确信，强调信心和确定性，常指系统运行良好或未来表现可靠
  - The SLA provides **assurance** that the service will have 99.9% uptime.
  - SLA 提供了服务将保持 99.9% 可用时间的**保证**。

- <书>：保障、防护措施，强调预先建立的保护机制或预防手段
  - Automated rollback is a key **safeguard** against failed deployments.
  - 自动回滚是防止部署失败的关键**保障**措施。
## 层面（level / layer / aspect / dimension）

- <通>：层面、层级，最通用，指**抽象的**层次或范围
  - At the system **level**, we need better monitoring.
  - 在系统**层面**，我们需要更好的监控。

- <书>：层面、层次，指**架构或概念**上的分层结构
  - This issue exists at the application **layer**, not the network.
  - 这个问题存在于应用**层面**，而不是网络层。

- <通>：层面、方面，指某个特定的**角度或侧面**
  - We need to consider the security **aspect** of this design.
  - 我们需要考虑这个设计的安全**层面**。

- <书>：维度、层面，强调从**不同角度或度量**来审视
  - We should evaluate this from a performance **dimension**.
  - 我们应该从性能**维度**来评估这一点。
## 高峰（peak / rush / spike / surge）

- <通>：高峰、峰值，指**流量、负载或活动**达到最高点的时段或数值
  - The system handles 10k QPS during **peak** hours.
  - 系统在**高峰**时段处理 1 万 QPS。

- <口>：高峰期、繁忙时段，特指**交通或人流量**大的时间段
  - Avoid deploying during **rush** hour to minimize risk.
  - 避免在**高峰**时段部署，以降低风险。

- <通>：尖峰、突刺，指短时间内**骤然升高又快速回落**的瞬时高峰
  - We saw a **spike** in error rate right after the release.
  - 发布后我们看到了错误率的**尖峰**。

- <书>：激增、浪潮，指**持续一段时间**的快速上涨浪潮
  - A **surge** in traffic overwhelmed the auto-scaling policy.
  - 流量的**激增**压垮了自动扩缩容策略。
## 卡顿（lag / jank / stutter / freeze / stall）

- <口>：卡顿、延迟，最通用，指响应变慢、有明显延迟感的现象
  - Users are complaining about **lag** when scrolling through the feed.
  - 用户反馈滚动信息流时有**卡顿**。

- <通>：卡顿、不流畅，**前端/渲染领域**专业术语，指掉帧、不流畅的卡顿感
  - The **jank** is noticeable when animating complex SVG elements.
  - 在动画复杂 SVG 元素时能明显感觉到**卡顿**。

- <口>：顿挫、不流畅，指不连续、跳帧式的卡顿感
  - The **stutter** in the animation is caused by garbage collection pauses.
  - 动画的**顿挫卡顿**是由垃圾回收停顿引起的。

- <通>：卡死、僵住，指完全无响应、画面定格的状态
  - A **freeze** occurred when the worker thread was blocked indefinitely.
  - 工作线程被无限阻塞时发生了**卡死**。

- <书>：停滞、阻塞，指流程被阻塞、无法向前推进的状态
  - I/O **stalls** are the main bottleneck in this data pipeline.
  - I/O **阻塞**是这个数据管道的主要瓶颈。
## 支柱（pillar / backbone / mainstay / cornerstone / anchor）

- <通>: 支柱、核心支撑，比喻系统中不可或缺的基础组件
  - The database is the **pillar** of our data infrastructure.
  - 数据库是我们数据基础设施的**支柱**。

- <通>: 主干、脊梁，强调承担主要功能或负荷的核心部分
  - The authentication service is the **backbone** of our microservices architecture.
  - 认证服务是我们微服务架构的**主干**。

- <书>: 主心骨、主要依赖，强调被广泛依赖的关键要素
  - Logging and monitoring are the **mainstay** of production reliability.
  - 日志和监控是生产可靠性的**主心骨**。

- <书>: 基石、基础，强调其他部分都建立在其之上
  - Strong typing is a **cornerstone** of this language's safety guarantees.
  - 强类型是该语言安全保证的**基石**。

- <书>: 锚点、固定点，强调提供稳定性和参照点
  - The configuration service serves as an **anchor** for all environment settings.
  - 配置服务是所有环境设置的**锚点**。
## 偏移 / 偏差（offset / bias / deviation）

- **人为抵消、补偿修正**，最通用万能词
  - We need to **offset** the increased latency by optimizing the cache.
  - *我们需要通过优化缓存来**抵消**延迟的增加。*

- **系统性偏向某一侧**，强调不均衡的倾向
  - The data has a **bias** toward older users.
  - *这些数据**偏向**于年龄较大的用户。*

- **与标准或预期的偏离**，强调可度量的偏差值
  - We need to investigate the **deviation** between the test and production metrics.
  - *我们需要调查测试和生产指标之间的**偏差**。*


## 泄露 / 漏洞（leak / breach / vulnerability）

- **数据或资源从内部无意流出**，最常用
  - We found a memory **leak** in the background worker.
  - *我们发现后台任务有一个内存**泄漏**。*

- **防线被攻破、外部入侵成功**，侧重安全边界被穿透
  - The company suffered a security **breach** last month.
  - *公司上个月遭受了一次安全**入侵**。*

- **系统自身存在的弱点或缺陷**，可被利用但尚未一定发生攻击
  - We patched a critical **vulnerability** in the authentication module.
  - *我们修补了认证模块中的一个关键**漏洞**。*


## 位置 / 地点（place / location / spot）

- **泛指场所或空间**，最通用最模糊，可物理可抽象
  - Let's find a **place** to deploy the staging environment.
  - *我们找个**地方**部署 staging 环境吧。*

- **精确的坐标或地址**，强调定位而非空间本身
  - The config file **location** is `/etc/app/config.yaml`.
  - *配置文件的**位置**是 `/etc/app/config.yaml`。*

- **具体的点或精准位置**，常指问题发生处或需关注的焦点
  - I found the exact **spot** where the memory leak is happening.
  - *我找到了内存泄漏发生的具体**位置**。*


## 想法 / 主意 / 观点（thought / idea / point）

- **思考后的念头**，侧重内心思索过程，未必成型或完整
  - I share my **thought** on this trade-off problem.
  - *我分享我对于这个取舍问题的**想法**。*

- **可落地的构想或方案点子**，能拿来尝试或实施
  - We have a new **idea** to reduce memory overhead.
  - *我们有一个降低内存开销的新**构想**。*

- **讨论中的论点或核心要点**，用于支撑论证
  - The main **point** is to avoid shared state mutation.
  - *核心**要点**是要避免共享状态发生变更。*


## 临时数据（placeholder / stub / dummy / mock / seed）*

> *本组因技术场景细分，保留5词以覆盖完整工程语境

- **占位符**：占住位置，等待后续替换，不一定可执行
  - We use a **placeholder** for the unreleased configuration field.
  - *我们为尚未发布的配置字段使用一个**占位符**。*

- **函数/接口桩**：具备签名，极简实现，用于对接未完成下游
  - Write an API **stub** so the frontend can proceed development.
  - *编写 API **桩**，让前端可以继续开发工作。*

- **哑数据**：无业务含义，仅填充结构保证格式合法
  - Fill the table with **dummy data** for UI layout debugging.
  - *向表中填充**哑数据**用于 UI 布局调试。*

- **模拟数据**：高仿真实业务形态，用于测试和调试
  - Generate realistic **mock data** to test the aggregation logic.
  - *生成贴近真实的**模拟数据**，用来测试聚合逻辑。*

- **种子数据**：系统初始化时预置的基础基准数据
  - Load **seed data** on database initialization.
  - *数据库初始化时加载**种子数据**。*


## 利弊 / 优劣（upside/downside / benefit/drawback / advantage/disadvantage）

- **方案本身的附带好处与副作用代价**，偏整体效果评估
  - The **upside** is lower memory consumption. The **downside** is higher CPU overhead.
  - ***好处**是内存消耗更低；**弊端**是 CPU 开销变高。*

- **实际收益 vs 方案固有的天生短板**，技术文档首选搭配
  - One key **benefit** is simplified deployment. Its main **drawback** is increased network latency.
  - *一个核心**益处**是部署简化；它主要**短板**是网络延迟增加。*

- **多方案相互比较得出的优势与劣势**，最通用
  - Compared with polling, event-driven model has several **advantages**. Its **disadvantages** include higher code complexity.
  - *对比轮询，事件驱动模型具备若干**优势**；它的**劣势**包含更高代码复杂度。*


## 前提 / 先决条件（premise / precondition / prerequisite）

- **逻辑假设、推演的出发点**，侧重观念上的预设
  - This design is built on the **premise** that latency is acceptable.
  - *该方案建立在延迟可以接受这一**前提**之上。*

- **执行前必须满足的系统状态**，运行时前置状态，程序断言高频
  - Check **precondition** before entering critical section.
  - *进入临界区前检查**先决条件**。*

- **事先必须具备的资源或组件**，想要做 A 必须先具备 B
  - Sufficient memory is a **prerequisite** for enabling this feature.
  - *充足内存是开启该功能的**必备前提**。*


## 量 / 数量（count / amount / volume / quantity）

- **可数个体的个数**，强调有多少项、多少条
  - Check the **count** of pending tasks.
  - *检查待处理任务的**数目**。*

- **不可数抽象量**（时间、内存、工作量）
  - A large **amount** of memory is consumed.
  - *消耗大量**内存**。*

- **体量、流量、吞吐规模**，侧重批量数据流
  - The service handles high request **volume**.
  - *该服务处理高请求**体量**。*

- **泛指总量**，书面通用，可数不可数均可
  - Limit the **quantity** of concurrent connections.
  - *限制并发连接的**数量**。*


## 状态（state / status）

- **内部状态、数据快照**：组件/系统/变量内部维护的数据
  - React components keep their own internal **state**.
  - *React 组件维护自身内部**状态**。*

- **对外报告的运行标识**：请求/任务/接口的执行情况
  - Check the **status** of the network request.
  - *检查网络请求的**状态**。*
## 方案（solution / proposal / schema）

- 解决方案，用来解决具体问题的成熟办法
  - This is an effective **solution** to avoid race conditions.
  - 这是避免竞态条件的有效**方案**。

- 提议、提案，仍在讨论阶段，待评审的构想
  - This syntax comes from an ES **proposal**.
  - 该语法来自一项 ES **提案**。

- 模式、架构定义，描述数据结构字段约束
  - The **schema** defines the format of input JSON data.
  - 这个 **schema** 定义输入 JSON 数据的格式。


## 模式（pattern / mode / model）

- 模式，反复复用的代码范式、设计套路
  - Observer is a common design **pattern**.
  - 观察者是一种常见的**设计模式**。

- 模式，程序运行档位、操作模式、开关状态
  - JavaScript runs in strict **mode**.
  - JavaScript 在严格**模式**下运行。

- 模型，数据层抽象，描述数据结构业务实体
  - This **model** describes the shape of user data.
  - 这个**模型**描述用户数据的结构。